@model IEnumerable<SGI_Philips.Models.Puesto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombrePuesto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombrePuesto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.puestoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.puestoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.puestoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
