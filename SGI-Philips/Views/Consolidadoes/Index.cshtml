@model IEnumerable<SGI_Philips.Models.Consolidado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.anioConsolidado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ventasConsolidado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.anioConsolidado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ventasConsolidado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.productoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mes.mesID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.consolidadoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.consolidadoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.consolidadoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
