@model IEnumerable<SGI_Philips.Models.Mes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreMes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreMes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.mesID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.mesID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.mesID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
